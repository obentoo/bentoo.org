# Cloudflare Pages production env
PUBLIC_SITE_URL=https://bentoo.org
PUBLIC_BUTTONDOWN_USERNAME=bentoo
PUBLIC_CF_ANALYTICS_TOKEN=

# Cloudflare Pages preview env (only override if different)
# PUBLIC_SITE_URL=https://preview.bentoo.org

# Build env (set automatically by CF Pages; documented for reference)
# CF_PAGES_BRANCH=main     # -> PUBLIC_IS_PRODUCTION=true at build time
# NODE_VERSION=22
